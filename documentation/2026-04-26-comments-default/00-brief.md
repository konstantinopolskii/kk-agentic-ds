---
session: 2026-04-26-comments-default
stage: 0
role: user
input: conversation
output: raw ask captured
gate: passed to stage 1
---

## Raw input

> I want it to be applied to any page where we have the comments. And if we don't have comments on the page, I want to have them. That's a rule by default.

## Revert point

`v1.6.1` at `268d0b6`. Roll back is `git reset --hard v1.6.1`.
